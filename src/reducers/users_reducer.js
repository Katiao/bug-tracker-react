/* const users_reducer = (state, action) => {
  if (action.type === "NEW_JWT") {
    return { ...state, jwt: action.payload };
  }
  return state;
  //throw new Error(`No Matching "${action.type}" - action type`);
};

export default users_reducer;
 */
